# ============================================================
# hello.hls — Your first program in Hieu Louis
# ============================================================

fn greet(name: str) -> str {
    return "Hello, " + name + "!"
}

fn main() -> int uses IO {
    println(greet("Hieu Louis"))
    println("A high-security programming language, compiled native.")
    println("Compiled by a compiler written in HLS itself.")
    return 0
}
