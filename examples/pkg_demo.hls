# examples/pkg_demo.hls — Stage 13 demo: a "package" entry point.
#
# This file demonstrates the structure a real `hls-pkg` entry point
# would take. To actually run it via `hls-pkg`, you would:
#
#   $ cd /tmp
#   $ python3 /path/to/tools/hls-pkg.py init mypkg
#   $ cd mypkg
#   $ python3 /path/to/tools/hls-pkg.py add std.str \
#       https://github.com/mhieuhonda/hieu-louis-lang.git std/str.hls
#   $ # edit hls-pkg.toml to set effects.allowed = ["IO"]
#   $ python3 /path/to/tools/hls-pkg.py lock
#   $ python3 /path/to/tools/hls-pkg.py audit
#   $ python3 /path/to/tools/hls-pkg.py build
#
# (You'd also replace main.hls with this file's content.)

import "std.str"

fn greet(name: str) -> str {
    return str_repeat("Hello, ", 1) + name + "!"
}

fn main() -> int uses IO {
    let names: list[str] = ["Hieu", "Louis", "World"]
    for n: str in names {
        println(greet(n))
    }
    return 0
}
