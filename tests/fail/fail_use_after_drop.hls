# fail_use_after_drop — Stage 8-alpha: using a dropped binding must be rejected
fn main() -> int uses IO {
    let s: str = "hello"
    drop(s)
    println(s)            # error: use of moved value
    return 0
}
