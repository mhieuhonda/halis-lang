# fail_stage86_irq_vector_twice.hls — Stage 86: a handler services exactly
# one vector, so two `#[irq_handler(N)]` on one function is a merge
# mistake rather than a second binding.
#[irq_handler(14)]
#[irq_handler(15)]
fn handler(frame: list[int]) -> void {
    return
}

fn main() -> int {
    return 0
}
