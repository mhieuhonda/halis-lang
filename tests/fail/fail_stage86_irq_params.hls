# fail_stage86_irq_params.hls — Stage 86: a handler takes exactly one
# parameter — the pointer to the frame the stub saved. Two parameters
# means the stub's register save does not match the signature, and the
# second one is read out of whatever the interrupted code left in rsi.
#[irq_handler(14)]
fn handler(frame: list[int], extra: int) -> void {
    return
}

fn main() -> int {
    return 0
}
