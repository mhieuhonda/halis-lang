# Methods are not supported yet (self is a refcounted receiver).
struct Counter {
    n: int,
}

impl Counter {
    #[tail_call]
    fn tick(self: Counter) -> int {
        if self.n <= 0 {
            return 0
        }
        return 0
    }
}

fn main() -> int {
    return 0
}
