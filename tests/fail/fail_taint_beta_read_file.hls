# Stage 10-beta: read_file_tainted returns tainted[str] — must sanitise
# before passing to a sink. This MUST be rejected by the checker.
fn main() -> int uses Fs, IO {
    let content: tainted[str] = read_file_tainted("examples/data.txt")
    println(content)
    return 0
}
