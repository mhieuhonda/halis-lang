# fail_contract_ensures_void.hls — an ensures clause on a void function
# has no `result` to talk about.
fn f(n: int)
    requires n > 0
    ensures result > 0
{
    return
}
fn main() -> int {
    f(1)
    return 0
}
