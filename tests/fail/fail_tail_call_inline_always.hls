# #[tail_call] + #[inline(always)]: the loop transform and call-site
# inlining contradict each other. Must be rejected at parse time.
#[inline(always)]
#[tail_call]
fn f(n: int) -> int {
    if n == 0 {
        return 0
    }
    return f(n - 1)
}

fn main() -> int {
    return f(10)
}
