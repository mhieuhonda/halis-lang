# fail_stage84_link_script_nosections.hls — Stage 84: a script with no
# SECTIONS block does not describe an image layout, so every
# `#[section]` in the crate would be placed by gcc's default script
# instead of the author's.
#![link_script("../stage84/no_sections.ld")]

fn main() -> int {
    return 0
}
