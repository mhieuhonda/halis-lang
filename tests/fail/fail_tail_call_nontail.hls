# The recursive call is NOT in tail position: its result feeds an
# operator before being returned. The verifier must reject it.
#[tail_call]
fn f(n: int) -> int {
    if n <= 0 {
        return 0
    }
    return f(n - 1) + 1
}

fn main() -> int {
    return f(10)
}
