# fail_freestanding_std_import.hls — Stage 77: `import "std.*"` is
# rejected in `#![freestanding]` mode (the std module is disabled).
#![freestanding]

import "std.str"

fn main() -> int {
    return 0
}
