# fail_asmreg_dup_clause.hls — Stage 83: one asm! carries at most one
# clobber(...) clause — a second clause is almost always a merge
# mistake, and the checker refuses to guess the intended list.
fn main() -> int {
    asm!("nop", clobber("rcx"), clobber("r11"))
    return 0
}
