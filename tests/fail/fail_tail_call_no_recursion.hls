# #[tail_call] with no recursive call at all — nothing to turn into a
# loop; the attribute is a pointless assertion. Must be rejected.
#[tail_call]
fn f(n: int) -> int {
    if n <= 0 {
        return 0
    }
    return n
}

fn main() -> int {
    return f(10)
}
