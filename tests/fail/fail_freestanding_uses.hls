# fail_freestanding_uses.hls — Stage 77: a `uses` clause is
# rejected in `#![freestanding]` mode (no capabilities; every
# function must be pure).
#![freestanding]

fn main() -> int uses IO {
    println("hi")
    return 0
}
