# This should fail: match is not exhaustive (missing Blue).
enum Color {
    Red,
    Green,
    Blue
}

fn color_name(c: Color) -> str {
    return match c {
        Color.Red => "red",
        Color.Green => "green"
    }
}

fn main() -> int {
    return 0
}
