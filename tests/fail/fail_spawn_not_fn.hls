# fail_spawn_not_fn.hls — the first spawn() argument must be a function
# NAME (an identifier), not an arbitrary expression.
fn worker(n: int) -> int { return n }
fn main() -> int uses IO, Conc {
    let n: int = 5
    let t: Task[int] = spawn(n + 1)
    return 0
}
