# fail_freestanding_float_mod.hls — Stage 77: float `%` lowers
# to fmod (no freestanding implementation).
#![freestanding]

fn main() -> int {
    let x: float = 5.5 % 2.0
    if x == 1.5 {
        return 0
    }
    return 1
}
