# fail_asmreg_clobber_dup.hls — Stage 83: duplicate clobber entries are
# a typo-shaped mistake (the asm silently accepts them). The checker
# rejects exact and alias duplicates alike ("rcx" twice here).
fn main() -> int {
    asm!("nop", clobber("rcx", "rcx", "r11"))
    return 0
}
