# fail_stage85_boot_notident.hls — Stage 85: the protocol is a bare
# identifier, not a string. `#![boot_header("limine")]` is a different
# (and meaningless) shape, so it is rejected rather than coerced.
#![boot_header("limine")]

fn main() -> int {
    return 0
}
