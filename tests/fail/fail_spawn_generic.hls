# fail_spawn_generic.hls — spawning a generic function is not supported
# yet (wrap it in a non-generic function instead).
fn identity[T](x: T) -> T { return x }
fn main() -> int uses IO, Conc {
    let t: Task[int] = spawn(identity, 5)
    return 0
}
