# fail_spawn_args.hls — argument count must match the target fn's params.
fn worker(a: int, b: int) -> int { return a + b }
fn main() -> int uses IO, Conc {
    let t: Task[int] = spawn(worker, 1)
    return 0
}
