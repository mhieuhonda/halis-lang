# Stage 30: #[stack] violated — reassigning the binding replaces the
# frame-resident storage.
fn main() -> int {
    #[stack]
    let mut xs: list[int] = [1, 2, 3]
    xs = [4, 5]
    return xs.len()
}
