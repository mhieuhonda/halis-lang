# fail_panic_bad_ret.hls — Stage 81: a `#[panic_handler]` fn that
# does not return `void` is rejected (the hook ignores any result;
# a value-returning handler would silently drop its answer).
#[panic_handler]
fn panic_handler(msg: str) -> int {
    return 0
}

fn main() -> int {
    return 0
}
