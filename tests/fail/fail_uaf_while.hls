fn uaf_in_while(xs: list[int]) -> void {
    let mut i: int = 0
    while i < 1 {
        let _: list[int] = take(xs)
        i = i + 1
    }
    let _: int = xs[0]
}
fn main() -> int {
    return 0
}
