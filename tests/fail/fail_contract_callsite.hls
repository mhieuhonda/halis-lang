# fail_contract_callsite.hls — a requires that evaluates to FALSE for
# literal arguments is a compile error at the call site.
fn div(a: int, b: int) -> int
    requires b != 0
{
    return a / b
}
fn main() -> int {
    let x: int = div(10, 0)
    return 0
}
