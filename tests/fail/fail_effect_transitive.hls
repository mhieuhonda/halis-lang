# error: indirectly calling an IO function without declaring uses IO
fn log_value(x: int) -> int uses IO {
    println("log " + x.to_str())
    return x
}

fn middleman(x: int) -> int {
    return log_value(x)
}

fn main() -> int {
    return middleman(1)
}
