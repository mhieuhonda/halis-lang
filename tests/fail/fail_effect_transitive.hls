# lỗi: goi gian tiep ham IO ma khong khai bao uses IO
fn ghi(x: int) -> int uses IO {
    println("ghi " + x.to_str())
    return x
}

fn trung_gian(x: int) -> int {
    return ghi(x)
}

fn main() -> int {
    return trung_gian(1)
}
