# fail_panic_dup_handler.hls — Stage 81: two `#[panic_handler]` fns
# in one program are rejected (the runtime hook is a single pointer;
# two handlers would be ambiguous).
#[panic_handler]
fn panic_handler(msg: str) -> void {
}

#[panic_handler]
fn second_handler(msg: str) -> void {
}

fn main() -> int {
    return 0
}
