# fail_take_moved — Stage 8-alpha: taking from a moved binding is illegal
fn main() -> int uses IO {
    let s: str = "hello"
    drop(s)
    let y: str = take(s)  # error: use of moved value
    return 0
}
