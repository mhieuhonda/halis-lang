# fail_freestanding_sin.hls — Stage 77: libm builtins have no
# freestanding lowering (the prelude provides no libm).
#![freestanding]

fn main() -> int {
    let x: float = math_sin(0.0)
    if x == 0.0 {
        return 0
    }
    return 1
}
