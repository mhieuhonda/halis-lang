# fail_stage85_boot_protocol.hls — Stage 85: `#![boot_header]` takes a
# protocol name, and only the two the C backend can emit. A typo would
# otherwise be accepted and produce an image with no header at all.
#![boot_header(uefi)

fn main() -> int {
    return 0
}
