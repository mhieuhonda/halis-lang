# fail_stage84_section_dup.hls — Stage 84: a function has exactly one
# output section, so a second #[section] is a merge mistake rather than
# a second placement.
#[section(".text.boot")]
#[section(".text.isr")]
fn stub(x: int) -> int pure {
    return x
}

fn main() -> int {
    return stub(1)
}
