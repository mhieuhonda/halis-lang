# A refcounted let binding: it would need release at the loop jump,
# which the verified transform forbids. Must be rejected.
#[tail_call]
fn f(n: int) -> int {
    let s: str = "x"
    if n <= 0 {
        return 0
    }
    return f(n - 1)
}

fn main() -> int {
    return f(10)
}
