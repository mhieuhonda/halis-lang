# fail_contract_unknown_var.hls — only PARAMETERS are in scope inside a
# contract (plus `result` in ensures).
fn g() -> int { return 1 }
fn f(n: int) -> int
    requires m > 0
{
    return n
}
fn main() -> int {
    return 0
}
