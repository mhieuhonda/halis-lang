# The recursive call's result is bound to a variable first — the call
# is not the entire return expression. The verifier must reject it.
#[tail_call]
fn f(n: int) -> int {
    if n <= 0 {
        return 0
    }
    let x: int = f(n - 1)
    return x
}

fn main() -> int {
    return f(10)
}
