# fail_spawn_effect.hls — spawn carries the Conc effect; a function that
# spawns without declaring `uses Conc` must be rejected.
fn worker() -> int { return 1 }
fn main() -> int uses IO {
    let t: Task[int] = spawn(worker)
    return 0
}
