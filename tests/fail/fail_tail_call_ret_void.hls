# void return: a tail call must feed a value back through the loop.
#[tail_call]
fn countdown(n: int) -> void {
    if n == 0 {
        return
    }
    return countdown(n - 1)
}

fn main() -> int {
    return 0
}
