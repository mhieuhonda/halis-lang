# fail_spawn_arg_type.hls — spawn argument types are checked like calls.
fn worker(a: int) -> int { return a }
fn main() -> int uses IO, Conc {
    let t: Task[int] = spawn(worker, "not an int")
    return 0
}
