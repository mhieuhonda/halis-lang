# Generic functions are not supported yet (the codegen emits
# per-instantiation bodies; the tail-loop keying is per source fn).
#[tail_call]
fn f[T](n: int, acc: T) -> int {
    if n <= 0 {
        return 0
    }
    return f[T](n - 1, acc)
}

fn main() -> int {
    return f[int](10, 0)
}
