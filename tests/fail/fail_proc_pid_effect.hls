# error: calling proc_pid without declaring uses Proc (the process
# identity is process-state access — the same effect family as
# env_get; Stage 57, v0.76.0-alpha).
fn main() -> int {
    let pid: int = proc_pid()
    return 0
}
