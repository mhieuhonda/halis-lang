fn uaf_in_if(c: bool, xs: list[int]) -> void {
    if c {
        let _: list[int] = take(xs)
    }
    # xs is potentially moved here (if c was true).
    # The soundness fix rejects this.
    let _: int = xs[0]
}
fn main() -> int {
    return 0
}
