# fail_stage84_align_dup.hls — Stage 84: #[align(N)] may appear once per
# function. Two alignments do not compose — the later one would silently
# replace the earlier one.
#[align(64)]
#[align(4096)]
fn stub(x: int) -> int pure {
    return x
}

fn main() -> int {
    return stub(1)
}
