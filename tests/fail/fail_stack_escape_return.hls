# Stage 30: #[stack] violated — the binding escapes via return.
# A stack-allocated value can NEVER outlive its creating frame; the
# checker names the escape site.
fn make() -> list[int] {
    #[stack]
    let xs: list[int] = [1, 2, 3]
    return xs
}
fn main() -> int {
    return make().len()
}
