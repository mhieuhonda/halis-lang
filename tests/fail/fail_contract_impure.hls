# fail_contract_impure.hls — contract expressions must be pure: calling
# a user function inside a requires is rejected.
fn helper(n: int) -> int { return n * 2 }
fn f(n: int) -> int
    requires helper(n) > 0
{
    return n
}
fn main() -> int {
    return 0
}
