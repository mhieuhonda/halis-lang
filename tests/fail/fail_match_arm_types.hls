# This should fail: match arm types differ.
enum Color {
    Red,
    Green,
    Blue
}

fn color_value(c: Color) -> int {
    return match c {
        Color.Red => 1,
        Color.Green => "two",
        Color.Blue => 3
    }
}

fn main() -> int {
    return 0
}
