# fail_double_drop — Stage 8-alpha: dropping an already-moved binding is illegal
fn main() -> int uses IO {
    let s: str = "hello"
    drop(s)
    drop(s)               # error: use of moved value
    return 0
}
