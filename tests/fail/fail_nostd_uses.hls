# fail_nostd_uses.hls — Stage 78: a `uses` clause is rejected
# in `#![no_std]` mode (every function must be pure).
#![no_std]

fn tick() -> int uses Clock {
    return clock_ms()
}

fn main() -> int {
    return tick()
}
