# fail_task_unknown_method.hls — Task has exactly one method: join.
fn worker() -> int { return 1 }
fn main() -> int uses IO, Conc {
    let t: Task[int] = spawn(worker)
    let n: int = t.len()
    return 0
}
