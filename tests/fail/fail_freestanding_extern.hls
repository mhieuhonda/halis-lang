# fail_freestanding_extern.hls — Stage 77: `extern` blocks are
# rejected in `#![freestanding]` mode (no host calls).
#![freestanding]

extern "C" {
    fn puts(s: str) -> int uses IO
}

fn main() -> int {
    return 0
}
