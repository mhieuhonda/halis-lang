# fail_panic_bad_sig.hls — Stage 81: a `#[panic_handler]` fn whose
# parameter is not `str` is rejected (the C hook passes `hl_str*`).
#[panic_handler]
fn panic_handler(code: int) -> void {
}

fn main() -> int {
    return 0
}
