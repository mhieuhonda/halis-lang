# A refcounted parameter: the rebind at the jump would need release
# handling the verified transform forbids. Must be rejected.
#[tail_call]
fn f(n: int, s: str) -> int {
    if n <= 0 {
        return 0
    }
    return f(n - 1, s)
}

fn main() -> int {
    return f(10, "x")
}
