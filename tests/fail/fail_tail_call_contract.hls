# Contracts interpose cleanup (the ensures check at return) between
# the tail call and the jump. Must be rejected.
#[tail_call]
fn f(n: int) -> int requires n >= 0 {
    if n == 0 {
        return 0
    }
    return f(n - 1)
}

fn main() -> int {
    return f(10)
}
