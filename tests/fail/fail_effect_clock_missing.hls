# fail_effect_clock_missing — Stage 9-alpha: clock_ms requires `uses Clock`.
fn time_now() -> int {
    return clock_ms()   # error: requires effect 'Clock' not declared
}

fn main() -> int uses IO {
    println(time_now().to_str())
    return 0
}
