# #[tail_call] + #[irq_handler]: an interrupt frame must return via
# IRETQ, never jump. Must be rejected at parse time.
#[tail_call, irq_handler]
fn f(n: int) -> int {
    if n == 0 {
        return 0
    }
    return f(n - 1)
}

fn main() -> int {
    return 0
}
