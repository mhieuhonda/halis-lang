# fail_effect_reserved — Stage 9-alpha: `Net` is a reserved effect name.
# Using it is a parse error — there are no Net builtins yet.
fn fetch(url: str) -> str uses Net {
    return ""
}

fn main() -> int uses IO {
    println("unreachable")
    return 0
}
