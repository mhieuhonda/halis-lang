# fail_stage86_irq_param_scalar.hls — Stage 86: the frame parameter is a
# POINTER. An `int` parameter lowers to a C scalar, so there is no
# address to pass and the stub's `movq %rsp, %rdi` has nothing to
# declare.
#[irq_handler(14)]
fn handler(frame: int) -> void {
    return
}

fn main() -> int {
    return 0
}
