# fail_stage85_boot_dup.hls — Stage 85: one image carries one header.
# Two `#![boot_header]` declarations would leave the emission order —
# and therefore the bytes on the wire — ambiguous.
#![freestanding]
#![boot_header(limine)]
#![boot_header(multiboot2)]

fn main() -> int {
    return 0
}
