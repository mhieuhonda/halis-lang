# Stage 30: #[stack] violated — the binding escapes as a call argument.
fn sum(xs: list[int]) -> int {
    let mut t: int = 0
    for v: int in xs {
        t = t + v
    }
    return t
}
fn main() -> int {
    #[stack]
    let xs: list[int] = [1, 2, 3]
    return sum(xs)
}
