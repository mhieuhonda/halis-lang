# fail_asmreg_int_sse.hls — Stage 83: the mirror of the float rule —
# an int operand needs a general-purpose register; xmm names and the
# x/v SSE classes are float-only.
fn main() -> int {
    let mut x: int = 0
    asm!("nop", out("xmm0") x, options(nomem))
    return x
}
