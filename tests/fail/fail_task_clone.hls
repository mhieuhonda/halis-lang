# fail_task_clone.hls — clone() of a Task join handle is forbidden
# (single-consumer: join exactly once).
fn worker() -> int { return 1 }
fn main() -> int uses IO, Conc {
    let t: Task[int] = spawn(worker)
    let t2: Task[int] = clone(t)
    return 0
}
