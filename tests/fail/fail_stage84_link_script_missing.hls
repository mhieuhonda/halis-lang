# fail_stage84_link_script_missing.hls — Stage 84: `#![link_script]`
# naming a file that is not there means the whole `-T script` link never
# happens, silently. The compiler reads the script at check time and
# reports the path it resolved.
#![link_script("../stage84/not_here.ld")]

fn main() -> int {
    return 0
}
