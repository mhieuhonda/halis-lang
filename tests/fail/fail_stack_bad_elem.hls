# Stage 30: #[stack] requires a primitive element type — list[str]
# elements are reference-counted and must stay boxed.
fn main() -> int {
    #[stack]
    let xs: list[str] = ["a", "b"]
    return xs.len()
}
