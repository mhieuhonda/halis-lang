# fail_send_shared.hls — ch.send() of a bare variable read is a cross-
# thread sharing error (the same rule as spawn arguments).
fn main() -> int uses IO, Conc {
    let ch: Chan[str] = chan_new()
    let s: str = "payload"
    ch.send(s)
    return 0
}
