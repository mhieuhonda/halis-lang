# Stage 30: #[stack] violated — clone() reads the list and produces a
# heap copy; the source binding escapes into clone's argument.
fn main() -> int {
    #[stack]
    let xs: list[int] = [1, 2]
    let ys: list[int] = clone(xs)
    return ys.get(0)
}
