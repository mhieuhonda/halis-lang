# error: eprint is a taint sink (a tainted message written to the
# terminal enables ANSI-injection — the same threat print / println
# carry; Stage 56, v0.75.0-alpha).
fn main() -> int uses IO {
    let msg: tainted[str] = taint_mark("payload")
    eprint(msg)
    return 0
}
