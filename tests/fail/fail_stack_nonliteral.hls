# Stage 30: #[stack] requires a list-literal initializer (the capacity
# is fixed at compile time).
fn make() -> list[int] {
    let mut xs: list[int] = [1]
    xs.push(2)
    return xs
}
fn main() -> int {
    #[stack]
    let xs: list[int] = make()
    return xs.len()
}
