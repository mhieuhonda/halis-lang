# fail_stage86_irq_vector_range.hls — Stage 86: an x86-64 IDT has 256
# entries, so a vector outside 0..255 is not a vector. A loader that
# clamps it would install the handler somewhere nobody expected.
#[irq_handler(256)]
fn handler(frame: list[int]) -> void {
    return
}

fn main() -> int {
    return 0
}
