# fail_freestanding_late_attr.hls — Stage 77: a crate-level
# attribute after an item is rejected (it belongs at the top of the
# entry file).
fn helper() -> int {
    return 1
}

#![freestanding]

fn main() -> int {
    return helper()
}
