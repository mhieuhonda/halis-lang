# Expect: exit 101, panic "fb_new: width and height must be positive"
import "core.boot"

fn main() -> int {
    let f: Framebuffer = fb_new(1073741824, 0, 768, 4096, 32, FbKind.Rgb)
    if f.width != 0 {
        return 1
    }
    return 0
}
