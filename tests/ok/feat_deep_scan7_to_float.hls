# Deep-scan-7: "+1.5".to_float() must succeed (parity with C strtod
# and Python float()). The old code only stripped a leading `-`, so
# "+1.5".to_float() panicked on the `+`.
fn main() -> int uses IO {
    let v: float = "+1.5".to_float()
    println("v: " + v.to_str())
    return 0
}
